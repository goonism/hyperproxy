{
    "private": true,
    "scripts": {
        "bootstrap": "lerna bootstrap",
        "test": "lerna run test",
        "prepare": "lerna run prepare",
        "clean": "rimraf packages/**/lib",
        "watch": "lerna run watch --parallel",
        "pages": "gh-pages -d packages/hyperproxy-browser/public"
    },
    "devDependencies": {
        "lerna": "^2.5.1",
        "rimraf": "^2.6.2",
        "gh-pages": "^1.1.0"
    },
    "workspaces": [
        "packages/*"
    ]
}
